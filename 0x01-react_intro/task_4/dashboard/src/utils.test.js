import { getFullYear, getFooterCopy, getLatestNotification } from './utils'; // Assuming both files are in the same directory

describe('getFullYear', () => {
  // ... (test for getFullYear remains the same)
});

describe('getFooterCopy', () => {
  // ... (tests for getFooterCopy remain the same)
});

describe('getLatestNotification', () => {
  // ... (test for getLatestNotification remains the same)
});

