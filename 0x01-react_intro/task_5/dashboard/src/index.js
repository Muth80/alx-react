// Targeting the 'app' element in the HTML and adding your application logic.
import React from 'react';
import './YourFile.js';

const appElement = document.getElementById('app');
appElement.textContent = 'Hello, World!';

